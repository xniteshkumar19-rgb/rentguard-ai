"use client";

// AdminDashboard is a thin alias for the ML Churn Dashboard.
// Having a separate file keeps the import in LoginPage clean:
//   import AdminDashboard from '@/components/AdminDashboard';
export { AdminChurnDashboard as default } from "@/components/AdminChurnDashboard";
